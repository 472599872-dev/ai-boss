# AI招聘工作台（应用内）使用手册

## 1. 界面结构

应用为左右分栏：

- 左侧：功能控制台（扫描、岗位配置、候选人池、模板、飞书与用量）
- 右侧：BOSS 内嵌受控浏览器

---

## 2. 首次进入后的操作顺序

1. 先修改项目根目录下的 `app_config.json`  
2. 完成飞书登录  
3. 在右侧登录 BOSS 账号  
4. 左侧进入 `岗位配置`，填写岗位规则  
5. 回到 `投递人选` 或 `主动找人` 开始扫描  
6. 在 `候选人池` 查看结果并执行后续动作

---

## 3. 左侧模块说明

## 3.1 工作台

- 显示当前岗位、扫描状态、最近评分摘要
- 用于总览和快速切换模块

## 3.2 投递人选

入口：BOSS 沟通页（`/web/chat/index`）

可设置：
- 开始序号
- 结束序号
- 消息状态：全部 / 只看未读 / 只看已读

执行流程：
1. 逐位定位候选人  
2. 打开在线简历并读取  
3. 评分并写入候选人池  
4. 更新进度和评分输出

## 3.3 主动找人

入口：推荐牛人（`/web/chat/recommend` 或 frame 变体）

可设置：
- 自动打招呼开关
- 自动打招呼阈值

执行流程：
1. 逐卡片点开候选人  
2. 优先读取在线简历  
3. 评分  
4. 超阈值时自动打招呼

## 3.4 候选人池

按岗位分池管理，支持：
- 最近获取排序 / 评分排序
- 清空当前岗位
- 查看
- 右侧打开（定位 BOSS 对话）
- 看简历（解析后的在线简历）
- 索简历
- 淘汰

## 3.5 岗位配置

每个岗位独立配置：
- 岗位名称、城市、薪资、经验
- 必备条件、加分项、排除项
- 简历保存目录
- 默认沟通话术

保存后立即生效。

## 3.6 话术模板

- 维护常用沟通模板
- 可用于索简历等沟通动作

## 3.7 飞书与用量

- 查看当前飞书登录身份
- 展示当前配置对应的飞书 OAuth / 人员额度表 / 余额信息
- 人员额度表默认只需要 3 列：`人员`（飞书人员选择框）、`充值token`、`已使用情况`
- 查看今日本地 token 用量，以及云端的累计已充值 / 已用 / 剩余 token
- 手动触发重新登录、刷新用量、立即同步云文档
- Windows 版本可在这里查看在线更新状态、配置更新清单地址、手动检查更新

说明：
- 推荐把飞书和 Dify 参数统一维护在项目根目录的 `app_config.json`
- 后续改凭证或改限额时，直接改配置文件即可
- Windows 在线更新也走同一个配置文件，核心字段是 `windows_update_manifest_url`
- 只有已经登记在飞书人员额度表中的账号，才能登录成功
- 每次 AI 调用后，系统都会累计写回该账号的已用 token

---

## 3.8 Windows 在线更新

Windows 安装版支持远程更新：

1. 启动后自动检查，或点击工具栏 `检查更新`
2. 如果远程 `latest.json` 有更高版本，会提示下载
3. 下载完成后，应用会提示退出并启动新版安装器

注意：

- 当前只实现了 Windows 在线更新
- 远程清单必须包含版本号、安装器地址、`sha256` 和文件大小
- 如果清单地址没配置，客户端不会检查更新

---

## 4. 评分规则（当前）

评分基于“看简历”同源文本：

- 总分 = 必备(75) + 加分(15) + 完整度(10) - 风险扣分
- 风险扣分：轻 -5 / 中 -10 / 重 -20
- 命中排除项：分数限制在 20~40
- 封顶规则：
  - 必备<100%：最高89
  - 必备<80%：最高79
  - 必备<60%：最高69
- 100分仅在全部条件满足且无风险时出现

---

## 5. 扫描控制

- `开始扫描`：按当前筛选与序号执行
- `停止当前扫描`：当前候选人处理完后停止
- 执行 AI 评分前，会先读取当前用户当天累计 token，用于限额判断
- 状态区显示：
  - 正在处理第几位
  - 总数
  - 是否收到停止请求

---

## 6. 常见操作建议

- 第一次接入时，优先在 `飞书与用量` 中验证登录和多维表格同步是否正常
- 扫描前先确认右侧页面在正确入口（沟通/推荐牛人）
- 未读筛选切换后，建议等待列表刷新再扫描
- 评分异常时，先用候选人池 `看简历` 核对解析文本
- 自动打招呼前，先用少量样本验证阈值

---

## 7. 常见问题

## 7.1 只看未读没生效

- 确认已选择 `只看未读`
- 确认 BOSS 列表已刷新
- 再次启动扫描观察状态日志

## 7.2 在线简历没读全

- 检查是否成功打开在线简历弹层
- 重新扫描该候选人并看 `看简历` 内容

## 7.3 自动打招呼没触发

- 检查自动打招呼是否开启
- 检查分数是否达到阈值
- 检查简历是否读取成功

## 7.4 Token 统计不准

- 确认自建 Dify / 网关是否透传了 usage 字段
- 如果没有 usage，应用会改为估算 token
- 建议网关返回 `metadata.usage.prompt_tokens / completion_tokens / total_tokens`

## 7.5 token 用完了

- 先到 `飞书与用量` 查看云端的 `已充值 / 已用 / 剩余 token`
- 检查飞书人员额度表中该账号是否已经登记
- 如果 `已使用情况 >= 充值token`，系统会提示联系管理员充值
